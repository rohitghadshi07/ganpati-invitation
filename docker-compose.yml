services:
  ganpati-web:
    build: .
    container_name: ganpati-invitation
    ports:
      - "8080:80"
    volumes:
      # Mount source for live editing (optional — remove for production)
      - ./ganpati-invitation:/var/www/html
    restart: unless-stopped
